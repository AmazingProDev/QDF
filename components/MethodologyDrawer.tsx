"use client";

import { X } from "lucide-react";
import { BANDS, type Settings } from "@/lib/types/telecom";

const pct = (value: number) => `${(value * 100).toFixed(0)} %`;
export function MethodologyDrawer({ open, settings, onClose }: { open: boolean; settings: Settings; onClose: () => void }) {
  if (!open) return null;
  return <><div className="drawer-backdrop" onClick={onClose}/><aside className="methodology-drawer"><div className="drawer-title"><div><span>MÉTHODOLOGIE</span><h2>Calcul TRD / IEA</h2></div><button onClick={onClose} aria-label="Fermer"><X/></button></div><div className="methodology-body"><p className="methodology-intro">Le TRD mesure la part de la dégradation initiale réellement résorbée après action. L&apos;IEA synthétise les TRD débit et PRB ; il ne remplace jamais le statut de qualification AFTER.</p><section><h3>1. Périmètre de comparaison</h3><p>Le secteur est apparié par <b>Secteurs</b> après normalisation des espaces. Les bandes à évaluer sont celles déclarées dégradées ou chargées dans le fichier <b>BEFORE</b>. Le fichier AFTER ne redéfinit pas le problème initial.</p></section><section><h3>2. TRD Débit</h3><div className="formula-box"><b>Gap débit BEFORE</b><span>max(0, seuil DL − débit BEFORE)</span><b>Gap débit AFTER</b><span>max(0, seuil DL − débit AFTER)</span><b>TRD Débit</b><span>1 − (Gap AFTER / Gap BEFORE)</span></div><p>Une bande est mesurable seulement si elle était initialement dégradée et si son gap BEFORE est supérieur à zéro.</p><div className="thresholds">{BANDS.map((band) => <span key={band}>{band} : <b>{settings.dlThresholds[band]} Mbps</b></span>)}</div></section><section><h3>3. TRD PRB</h3><div className="formula-box"><b>Gap PRB BEFORE</b><span>max(0, PRB BEFORE − seuil PRB)</span><b>Gap PRB AFTER</b><span>max(0, PRB AFTER − seuil PRB)</span><b>TRD PRB</b><span>1 − (Gap AFTER / Gap BEFORE)</span></div><p>Le soulagement PRB est affiché en points : <b>PRB BEFORE − PRB AFTER</b>. Seuil PRB actuel : <b>{settings.prbThreshold} %</b>.</p></section><section><h3>4. IEA — indice d&apos;efficacité</h3><div className="formula-box one-line"><b>IEA</b><span>{pct(settings.throughputWeight)} × TRD Débit + {pct(settings.prbWeight)} × TRD PRB</span></div><p>Si un seul TRD est mesurable, l&apos;IEA reprend ce TRD. Si aucun ne l&apos;est, le résultat est « Non mesurable ».</p></section><section><h3>5. Qualification de l&apos;action</h3><table className="methodology-table"><tbody><tr><td>Très efficace</td><td>IEA ≥ {pct(settings.veryEffective)}</td></tr><tr><td>Efficace</td><td>{pct(settings.effective)} ≤ IEA &lt; {pct(settings.veryEffective)}</td></tr><tr><td>Amélioration partielle</td><td>{pct(settings.partial)} ≤ IEA &lt; {pct(settings.effective)}</td></tr><tr><td>Impact faible / non significatif</td><td>{pct(settings.regression)} ≤ IEA &lt; {pct(settings.partial)}</td></tr><tr><td>Régression</td><td>IEA &lt; {pct(settings.regression)}</td></tr></tbody></table></section><section><h3>6. Lecture de la qualification AFTER</h3><p><b>Statut de dégradation</b> (Normalisé, Persiste, Compromis…) vient du fichier AFTER. <b>Efficacité de l&apos;action</b> vient de l&apos;IEA. Un secteur peut donc être « Persiste » tout en étant « Efficace ».</p><p className="methodology-note">Comparaison indicative sur deux snapshots : pour attribuer une causalité forte, comparez des fenêtres BH homogènes et contrôlez trafic et utilisateurs.</p></section></div></aside></>;
}
