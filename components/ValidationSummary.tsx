import { CheckCircle2, AlertTriangle } from "lucide-react";
import type { ValidationSummary as Summary } from "@/lib/types/telecom";
export function ValidationSummary({ summary }: { summary: Summary }) { return <section className="validation"><div><span className="eyebrow">VALIDATION</span><h2>Structure détectée et prête à analyser</h2></div><div className="validation-stats"><div><b>{summary.beforeRows.toLocaleString("fr-FR")}</b><span>Lignes BEFORE</span></div><div><b>{summary.afterRows.toLocaleString("fr-FR")}</b><span>Lignes AFTER</span></div><div><b>{summary.common.toLocaleString("fr-FR")}</b><span>Secteurs communs</span></div><div><b>{summary.onlyBefore + summary.onlyAfter}</b><span>Non appariés</span></div></div><div className="validation-foot"><span>{["L1800","L2100","L2600","L800"].map((band) => summary.bands[band as keyof typeof summary.bands] ? <i key={band}><CheckCircle2 size={15}/>{band}</i> : <i className="warning" key={band}><AlertTriangle size={15}/>{band}</i>)}</span><small>Doublons : {summary.beforeDuplicates + summary.afterDuplicates}</small></div></section>; }
